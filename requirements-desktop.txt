# ============================================================
# SmartGastos — Dependencias del instalador desktop (PyInstaller)
# Subconjunto liviano: sin easyocr/torch.
# Fotos de boletas: RapidOCR + ONNX Runtime (latin). PDFs digitales: PyPDF2.
# Moondream sigue en Ollama como último recurso; el pipeline descarta alucinaciones.
# Pinokio sigue usando requirements.txt completo.
# ============================================================

fastapi==0.115.0
uvicorn==0.30.6
python-multipart==0.0.9

pydantic==2.9.2
pydantic-settings==2.5.2

sqlalchemy==2.0.35

requests==2.32.3
httpx==0.27.2

PyPDF2==3.0.1
Pillow==10.4.0
pytesseract==0.3.13
pdf2image==1.17.0

pandas==2.2.3
numpy==1.26.4
openpyxl==3.1.5

reportlab==4.2.5

python-dateutil==2.9.0
python-dotenv==1.0.1

# OCR de fotos (sin torch). opencv llega como dependencia de rapidocr.
# Primera ejecución descarga modelos latin a DATA_DIR/ocr_models (~20 MB).
rapidocr==3.3.1
onnxruntime==1.19.2
