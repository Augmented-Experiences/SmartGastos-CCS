[package]
name = "smartgastos"
version = "1.0.0"
description = "SmartGastos — gestión de gastos y contabilidad con IA local para PYMEs, de la Cámara de Comercio de Santiago (CCS)."
authors = ["Cámara de Comercio de Santiago"]
edition = "2021"
rust-version = "1.77"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sysinfo = "0.33"
ureq = { version = "2.12.1", default-features = false, features = ["native-tls"] }
zip = { version = "0.6.6", default-features = false, features = ["deflate"] }
tar = "0.4.43"
flate2 = { version = "1.0.35", default-features = false, features = ["rust_backend"] }

[[bin]]
name = "smartgastos"
path = "src/main.rs"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
